import { FilterproduitPipe } from './filterproduit.pipe';

describe('FilterproduitPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterproduitPipe();
    expect(pipe).toBeTruthy();
  });
});
